import {
  Timer,
  CalendarDays,
  BarChart3,
  Users,
  Settings,
  Home,
} from 'lucide-react';

export const NAV_CARD_ICONS = {
  timer: Timer,
  plan: CalendarDays,
  record: BarChart3,
  mate: Users,
  settings: Settings,
  home: Home,
};
